3 가지 방법
- (1) BFS + 시뮬레이션
- (2) 거리 계산 최적화
    순열이라고 생각했지만 콤비네이션을 쓴다. 콘돔이라고 말할뻔.
- (3) 백트래킹

현재 문제집의 카테고리는 브루트포스이므로 백트래킹 먼저 풀이
백트래킹 연습은 다양하게 많이 해두면 좋다.

문제가 너무 길어서 욕나온다.

사용되는 개념
- 거리 계산 : 맨허튼 거리 계산 공식 (abs(x1-x2) + abs(y1-y2))

주요 내용
- 궁수는 성에 위치
- 적은 아래로 1칸씩 이동



(3) 백트래킹
backtrack
- selected 라는 배열에 (`0 ~ k ~ N`) k 번째 로우에 대해 0,1,... m ... M 사이의 m 좌표를 대입해본 후
- 다음 row (i+1) 에서 또 selected 라는 배열에 k+1 번째 row 에 대해 0,1,... m ... M 사이의 m 좌표를 대입
- 위 과정을 반복하다가 len(selected) == 3 일때 simulate 를 수행
- i를 selected 넣어본후 backtrack, backtrack 이후 다시 i를 빼서 원상복구 (backtrack)

simulate
- len(selected) = 3 일때 거치는 함수
- find_target 을 통해 target 거리가 D 이하인 좌표들을 찾는데, find_target 에 대해서는 아래에서 설명
- find_target 으로 찾은 좌표들에 대해 0 으로 표시 후 `killed+=1` 처리
- 적은 아래로 1칸 이동 (문제에서 제시)

find_target
- 궁수의 위치 N,i (궁수는 성에 위치함)
- D 보다 가깝거나 같은 거리가 있을 때에만 좌표 return
- 거리 계산시 맨허튼 거리 계산 공식 사용

