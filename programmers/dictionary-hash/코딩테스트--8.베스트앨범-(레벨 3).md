
#programmers-lv3


# Link
https://school.programmers.co.kr/learn/courses/30/lessons/42579


# python (my)
```python
from collections import defaultdict


def solution(genres, plays):
    hit_genre = defaultdict(int)
    score_table = defaultdict(int)
    song_group = defaultdict(list)
    
    for i in range(len(genres)):
        hit_genre[genres[i]] += plays[i]
        song_group[genres[i]].append(i)
        
    popular_genres = sorted(hit_genre.items(), key=lambda x: x[1], reverse=True)
    
    answer = []
    for (genre, total) in popular_genres:
        most_hit_song = sorted(song_group[genre], key=lambda x:plays[x], reverse=True)
        answer += most_hit_song[:2]
    
    return answer
```



# python (옛날 옛적 풀었던 풀이)
ㅋㅋㅋ sort 막 날려서 쓰고 현란하게
```python
from collections import Counter
def solution(genres, plays):
    genreCnt = Counter()
    
    for g, p in zip(genres, plays):
        genreCnt[g] += p
    
    id_list = list(range(len(plays)))
    id_list.sort(key = lambda idx : plays[idx], reverse=True)
    id_list.sort(key = lambda idx : genreCnt[genres[idx]], reverse=True)
    
    answer = []
    
    top2Cnt = Counter()
    for idx in id_list:
        if top2Cnt[genres[idx]] < 2:
            top2Cnt[genres[idx]]+=1
            answer.append(idx)
    
    return answer
```


# python (책)
```python
from collections import defaultdict


def solution(genres, plays):
    summary = defaultdict(list)
    plays_summary = defaultdict(int)
    for index, (genre, play) in enumerate(zip(genres, plays)):
        summary[genre].append([index, play])
        plays_summary[genre] += play
        
    by_plays = sorted(plays_summary.items(), key=lambda x: -x[1])
    result = []
    
    for genre, _ in by_plays:
        songs = sorted(summary[genre], key=lambda x: -x[1])[:2]
        result.extend([song for song, _ in songs])
    return result
    
```
어째 좀더 어렵게 푸는 문제다.


