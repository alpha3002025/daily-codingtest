
#programmers-lv1


문제가 이상하게 나열하고 있길래 은근 쫄았는데 책에 비해 오히려 내가 푼게 더 깔끔한거였다.

# Link
https://school.programmers.co.kr/learn/courses/30/lessons/42840

# python
```python
def solution(answers):
    person1 = [1,2,3,4,5]
    person2 = [2,1,2,3,2,4,2,5]
    person3 = [3,3,1,1,2,2,4,4,5,5]
    
    count = [0,0,0]
    max_point = 0
    
    for i in range(len(answers)):
        if person1[i%len(person1)] == answers[i]:
            count[0]+=1
        if person2[i%len(person2)] == answers[i]:
            count[1]+=1
        if person3[i%len(person3)] == answers[i]:
            count[2]+=1
        
        pmax = max(count)
        max_point = max(max_point, pmax)
    
    
    return [i + 1 for i,c in enumerate(count) if max_point == c]
```
